#was r.enforce
PGM=r.carve

OBJS= main.o lobf.o line_dist.o write_sites.o enforce_ds.o

$(OBJS): enforce.h

EXTRA_CFLAGS= $(VECT_INCLUDE)

LIBRARIES = $(BITMAPLIB) $(LINKMLIB) $(VECTLIB) $(GISLIB) $(DATETIMELIB) 

$(BIN_CMD)/$(PGM): $(OBJS) $(LIBRARIES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBRARIES) $(MATHLIB) $(XDRLIB)

$(GISLIB): #
