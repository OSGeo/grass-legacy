.IH
.ce
\fB\s+2Index of Contributed Code\fP\s0
.sp 3
.ce 2
\fBThe following programs were contributed for the 5.0 GRASS release
.br
and are accompanied by on-line documentation.\fR
.sp 5
.IP "d.linegraph -" 16
Generates and displays simple line graphs in the active graphics monitor
display frame.
(GRASS Display Program)

.IP "f.acap -" 16
Calculates area-capacity for select areas of interest and shows
relationships between water surface elevation, water surface area, and 
volume of water.

.IP "f.detail -" 16
Functions like f.econ but outputs two detailed reports: economic damage
and depth of flooding.

.IP "f.econ -" 16
Takes as input the results of f.wsurf along with a user-supplied
vector map of building sites and two ASCII files of economic data
and generates a vector map of total damage.

.IP "f.input -" 16
Reads the results of the HEC-2 Water Surface Profile model and generates
a vector map of water surface evaluations at hydraulic sections.

.IP "f.reach -" 16
Provides floodwater statistics, including area extent of flooding,
average flood depth, and volume of water, calculated on a
reach-by-reach basis.

.IP "f.wsurf -" 16
Takes the results of f.input along with a raster terrain elevation map
and generates two raster maps 
describing the floodplain.

.IP "f.xsection -" 16
Provides an interactive means for digitizing new cross-sections to be
used as input to HEC-2 modeling.

.IP "i.texture -" 16
Calculates textural features on a raster file.
(GRASS Image Processing Program)

.IP "r.traj -" 16
Ballistic trajectory modeling program.

.IP "s.medp -" 16
Median polish for a GRASS sites list.
(GRASS sites)

.IP "s.to.rast -" 16
Converts a site file to a cell file.
(CCRS Contrib Program)

.IP "s.voronoi -" 16
Uses a GRASS sites list to produce a Voronoi diagram.

.IP "v.autocorr -" 16
Calculates spatial autocorrelation statistics for GRASS vector files.
(GRASS vector)

.IP "v.in.poly -" 16
Creates a vector file of polygons centered on given locations.
(GRASS Vector Program)

.IP "v.mkgrid -" 16
Creates a (binary) GRASS vector map of a user-defined grid.
(GRASS vector)

.IP "v.reg -" 16
Computes the latitude and longitude of the exterior and interior corner tic
marks for USGS 7.5' quad maps.

.IP "wrat -" 16
WRAT is an interactive module for evaluating water resources.
.br
.sp 3
.TL
\fBThe following programs were incorporated into contributed code 
in pre-5.0 GRASS releases and may/may not be accompanied by 
on-line documentation.\fR
.br
.LP
DOS.delete 

DOS.list

DOS.save

DOS.show

Darea

Dtrue

ISMann2dig

ISMcell2grd

ISMcell2scat

ISMcont2dig

ISMdig2ann

ISMgrd2cell

Mlulc.USGS

Mlulc.read

SG3d

d.6386.delete

d.6386.save

d.6386.show

d.distance

d.fix.ortho

d.text

d.vect.cats

grass.to.ipw

i.rvi.prediction

i.shape

m.6386

m.bsplit

m.dos

m.eigensystem

m.futil

m.geo

m.get.fips

m.get.stp

m.qcalc

m.regression

m.setproj

m.stp.proj

m.strip99s

p.hpx1300

r.gif

r.in.elas

r.in.erdas

r.in.miads

r.in.ppm

r.in.tang

r.in.utm

r.linear.regression

r.ndvi.model

r.out.ascii

r.out.ppm

r.out.ppm3

r.out.sites

r.rational.regression

r.reclass.scs

r.rvi

r.support

r.tiff

r.to.rlc

r.traj.data

s.makedots

s.to.vect

s.voronoi.shar

v.dump

v.export

v.extract

v.import

v.in.dlg.scs

v.in.gef

v.in.tiger.scs

v.make.subj

v.merge

v.mk_stats

v.mkquads

v.mkquads.scs

v.out.dlg.scs

v.out.gef

v.patch.scs

v.proj

v.psu

v.psu.subj

v.random

v.reclass

v.report

v.rmedge

v.scale.random

v.support

vs.digit
