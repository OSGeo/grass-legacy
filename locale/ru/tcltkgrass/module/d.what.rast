interface_build {
    {d.what.rast} 0
    {Определить значение категории растра в точке}
    {entry map {Растровый файл (имя[,имя,...]):} 0 +raster}
    {entry fs {Символ разделителя форм.вывода [пробел]:} 0 ""}
    {checkbox -1 {только в одной точке} "" -1}
    {checkbox -t {форматный вывод} "" -t}
}
