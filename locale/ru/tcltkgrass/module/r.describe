interface_build {
    {r.describe} 0
    {Выводит краткий список категорий растровой карты.}
    {entry map {Растровая карта:} 0 raster}
    {checkbox -1 {Формат одно знач. на строку.} "" -1}
    {checkbox -r {Только диапазон знач.} "" -r}
    {checkbox -q {без комм.} "" -q}
    {checkbox -d {Исп. текущ. регион.} "" -d}
}
