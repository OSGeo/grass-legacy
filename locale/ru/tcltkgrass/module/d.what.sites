interface_build {
    {d.what.sites} 0
    {Определят значение категорий для карты объектов.}
    {entry map {Карта объектов (name[,name,...]):} 0 +sites}
    {checkbox -1 {Опред. только один объект.} "" -1}
    {checkbox -q {без комм.} "" -q}
    {checkbox -t {краткий вывод.} "" -t}
}
