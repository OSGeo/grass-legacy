interface_build {
    {d.what.sites} 0
    {Определят значение категорий для карты объектов.}
    {entry sites {Карта объектов (имя[,имя,...]):} 0 sites}
    {checkbox -q {работать без комм.} "" -q}
}
