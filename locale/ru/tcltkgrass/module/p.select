interface_build {
    {p.select} 0
    {Выбирает устройство (принтер) для вывода на печать.}
    {entry painter {Выбрать устройство:} 0 paint}
    {checkbox -l {Список принтеров.} "" -l}
    {checkbox -p {Показать текущ. принтер.} "" -p}
    {checkbox -q {без комм.} "" -q}
}
