interface_build {
    {r.in.gdal} 0
    {Импорт GDAL-растра в бинарную растровую карту.}
    {entry input {Файл GDAL:} 0 file}
    {entry output {Растровая карта:} 0 raster}
    {entry title {Титул карты рез-та (выб.):} 0 ""}
    {entry band {Выбрать полосу (default все полосы):} 0 ""}
    {checkbox -o {Переписать проекцию (исп. проекцию области).} "" -o}
    {checkbox -e {Расширить границы области по нов. данным.} "" -e}
}
