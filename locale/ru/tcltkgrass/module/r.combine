interface_build {
    {r.combine} 1
    {Позволяет сочетать величины категорий нескольких растровых карт.}
    {entry < {Коммандный файл.} 0 file}
    {checkbox -s {Исп. символы (вместо графики)} "" -s}
}
