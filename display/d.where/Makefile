MODULE_TOPDIR = ../..

PGM = d.where

LIBES     = $(DISPLAYLIB) $(RASTERLIB) $(GPROJLIB) $(GISLIB)
DEPENDENCIES= $(DISPLAYDEP) $(RASTERDEP) $(GPROJLIBDEP) $(GISDEP)

include $(MODULE_TOPDIR)/include/Make/Module.make

default: cmd
