#DIGLIB = $(GISs)/src/mapdev/lib/libdig.a

PGM =	v.out.arc

OBJ =	Vexport_arc.o\
	write_areas.o\
	write_lines.o\
	write.o\
	writ_a_lines.o

EXTRA_CFLAGS = -I$(SRC)/mapdev/lib

$(BIN_ALPHA_CMD)/$(PGM): $(OBJ) $(DIGLIB)
	$(CC) -o $@  $(LDFLAGS) $(OBJ) $(DIGLIB) $(GISLIB) $(DIG_ATTLIB)

$(DIGLIB): #
$(GISLIB): #
