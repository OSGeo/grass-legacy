.TH Gline 2G
.SH NAME
Gline \- extract lines from cell file into digit file
.SH SYNOPSIS
.B Gline
[\fB-ab\fR] cell_file digit_file
.SH DESCRIPTION
.I Gline
scans the named
.I cell_file,
assumes that the non-zero cells represent lines of single-cell
width (such as produced by
.I Gthin,
and extracts vectors which follow the lines.
.I Gline
is capable of producing both ASCII and binary digit files.
.SH OPTIONS
.IP \fB-a\fR
create an ASCII digit file in the digit directory.
The file will have ".ascii" appended to the name
given on the command line.
.sp
.IP \fB-b\fR
create the named binary digit file in the digit directory.
.sp
The default is to produce a binary digit file only.  To produce
no output, use "-" alone.
.SH SEE ALSO
.I Gthin
- thin lines to single-cell width
.SH AUTHOR
Jean Ezell
.SH BUGS
.I Gline
may produce "extra" lines at some intersections.  If the 
input
.I cell_file
does not contain single-cell wide lines, the results
will be nonsense.  Known to crash.
