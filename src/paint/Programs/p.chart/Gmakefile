PGM=p.chart
INTERFACE = ../../Interface/applib/lib.a
EXTRA_CFLAGS = -I../../

LIST =	main.o

$(BIN_CMD)/$(PGM): $(LIST) $(INTERFACE) $(DEPGISLIB)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(INTERFACE) $(GISLIB) $(MATHLIB) $(XDRLIB)


$(INTERFACE):	# in case the library changes
$(DEPGISLIB):	# in case the library changes
