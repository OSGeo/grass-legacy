PGM=p.chart
INTERFACE = $(SRC)/paint/Interface/applib/lib.a
EXTRA_CFLAGS = -I../../

LIST =	main.o

$(BIN_INTER)/$(PGM): $(LIST) $(INTERFACE) $(GISLIB)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(INTERFACE) $(GISLIB) $(MATHLIB) $(XDRLIB)


$(INTERFACE):	# in case the library changes
$(GISLIB):	# in case the library changes
