PGM=r.sun
OBJ=main.o
SOURCES=main.c

LIBES=$(GISLIB) $(GPROJLIB) $(LINKMLIB)
DEPLIB=$(DEPGIS) $(DEPGPROJ) $(DEPLINKMLIB)

$(BIN_CMD)/$(PGM): $(OBJ) $(DEPLIB)
	$(CC) $(LDFLAGS) -o $@ $(OBJ) $(LIBES) $(MATHLIB) $(XDRLIB)
