PGM=r.sun
OBJ=main.o
SOURCES=main.c

LIBES=$(GPROJLIB) $(GISLIB) $(LINKMLIB)
DEPLIB=$(DEPGPROJLIB) $(DEPGIS) $(DEPLINKMLIB)

$(BIN_CMD)/$(PGM): $(OBJ) $(DEPLIB)
	$(CC) $(LDFLAGS) -o $@ $(OBJ) $(LIBES) $(MATHLIB) $(XDRLIB)
