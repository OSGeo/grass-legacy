Gslope.aspect [-v] elevation=input slope=output1 aspect=output2 format=degrees/persent
    Generates slope and aspect maps from elevation data.
    The elevation layer specified by the user must contain true
    elevation values, not rescaled or categorized data.
