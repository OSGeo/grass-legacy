PGM=polish

all: $(ETC)/$(PGM)

remove:
	rm -f lex.yy.c y.tab.c y.output

$(ETC)/$(PGM): main.o y.tab.o lex.yy.o
	$(CC) $(LDFLAGS) main.o y.tab.o lex.yy.o -o $@ $(LEXLIB)

y.tab.c y.tab.h: pol.y
	$(YACC) -d pol.y

lex.yy.c: pol.l y.tab.h
	$(LEX) -t pol.l > $@
