LIBES  = $(GISLIB)

SRCS   = main.c die.c
LIST   = main.o die.o

$(BIN_MAIN_CMD)/r.what: $(LIST) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)

$(LIBES): #

saber_src: 
	#load $(CFLAGS) $(SRCS) $(LIBES)
