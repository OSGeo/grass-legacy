LIBES  = $(DISPLAYLIB) $(RASTERLIB) $(GISLIB)

PGM    = r.profile

LIST   = main.o \
	read_rast.o


$(BIN_CMD)/$(PGM): $(LIST) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)

$(LIBES): #
