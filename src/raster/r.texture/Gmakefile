PGM=r.texture

OBJS = main.o h_measure.o

LIBES= $(GISLIB)
DEPLIBS= $(DEPGISLIB)

all: $(BIN_CMD)/$(PGM) 

$(BIN_CMD)/$(PGM): $(OBJS) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(MATHLIB) $(XDRLIB)

$(DEPLIBS): #
