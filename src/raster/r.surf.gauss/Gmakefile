PGM = r.surf.gauss

LIBES = $(GISLIB) $(VASKLIB) $(GMATHLIB)

OBJ =\
	main.o		\
	gaussurf.o

$(BIN_CMD)/$(PGM): $(OBJ) $(LIBES)
	 $(CC) $(LDFLAGS) -o $@ $(OBJ) $(LIBES) $(CURSES) $(MATHLIB) $(XDRLIB)

$(LIBES): #
