PGM=r.kappa

LIBES= $(GISLIB) $(VASKLIB)

OBJS = main.o\
	calc_kappa.o\
	mask.o\
	prt_hdr.o\
	prt_label.o\
	prt_mat.o\
	stats.o\
	sum.o

$(BIN_CMD)/$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(CURSES) $(MATHLIB) $(XDRLIB) $(EXTRA_FLAGS)

$(LIBES): #
