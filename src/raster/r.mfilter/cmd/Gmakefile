PGM = r.mfilter

LIST = \
	main.o\
	getfilt.o\
	getrow.o\
	apply.o\
	execute.o\
	perform.o\
	uppercase.o

$(BIN_MAIN_CMD)/$(PGM): $(LIST) $(GISLIB) $(ROWIOLIB)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(GISLIB) $(ROWIOLIB) $(MATHLIB) $(XDRLIB)

$(LIST): filter.h glob.h

$(GISLIB): # in case library changes
$(ROWIOLIB): # in case library changes
