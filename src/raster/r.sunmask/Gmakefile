PGM    = r.sunmask
LIBES  = $(GPROJLIB) $(GISLIB)
DEPLIBS= $(DEPGPROJLIB) $(DEPGISLIB)

LIST   = main.o solpos00.o g_solposition.o

$(BIN_CMD)/$(PGM): $(LIST) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES)	$(MATHLIB) $(XDRLIB)

$(DEPLIBS): #
