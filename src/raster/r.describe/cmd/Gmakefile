PGM=r.describe
LIBES  = $(GISLIB)
DEPLIBS= $(DEPGISLIB)

LIST   = main.o describe.o dumplist.o

$(BIN_CMD)/$(PGM): $(LIST) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)

$(DEPLIBS): #
