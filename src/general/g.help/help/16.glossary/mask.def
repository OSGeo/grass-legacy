

mask - The GRASS command which creates a \raster map\ that restricts
analysis to a particular geographic region designated by the user
("masking out" all other areas from analysis).  This map, named MASK,
can be altered, created or destroyed with the GRASS "mask" command.
