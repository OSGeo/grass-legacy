

r.combine - 

r.combine allows \category values\ from several \raster\ map layers to
be combined.  The program accepts commands that are similar to those 
used for \Boolean\ combinations (AND, OR, NOT) in order to overlay
user-selected groups of \categories\ from different raster map layers. 

Manual entry for \r.combine\
