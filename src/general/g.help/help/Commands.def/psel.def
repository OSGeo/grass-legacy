

p.select - 

p.select allows the user to select the device for GRASS hardcopy
output.  The user must select a device BEFORE running the other GRASS
hardcopy output functions (e.g., \p.map\, \p.labels\, \p.chart\).

Manual entry for \p.select\
