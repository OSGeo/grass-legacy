

g.copy - 

g.copy allows the user to copy existing data files from other mapsets to
the user's current \mapset\.  The files to be copied must exist in the
user's current mapset search path and \location\; output is sent to the
relevant data element directory(ies) under the user's current mapset.

Manual entry for \g.copy\
