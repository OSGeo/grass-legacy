

v.mkcontour - 

v.mkcontour creates a \vector\ map layer containing unlabelled contour
lines at user-specified intervals, based on \category values\ present in
a \raster\ map layer.

Manual entry for \v.mkcontour\
