

dcorrelate.sh - 

dcorrelate.sh is a C-shell script that graphically displays the results
of an \r.stats\ run on two raster map layers.  This shell script is
useful for highlighting the correlation (or lack of it) among data
layers.

Manual entry for \dcorrelate.sh\
