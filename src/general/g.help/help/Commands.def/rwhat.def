

r.what - 

r.what outputs the \category values\ and (optionally) the
\category labels\ associated with user-specified \locations\ on
\raster\ input map(s).  Locations are specified as geographic x,y
coordinates pairs (i.e., pair of \eastings\ and \northings\).  The
user can also (optionally) associate a \label\ with each location.

Manual entry for \r.what\
