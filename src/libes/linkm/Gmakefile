EXTRA_CFLAGS = $(PICFLAGS)

OBJ = \
	init.o\
	new.o\
	next.o\
	oom.o\
	dispose.o

#all: $(DEPLINKMLIB) $(LIBDIR)/linkm.h
all: $(DEPLINKMLIB)

$(DEPLINKMLIB): $(OBJ)
	$(SLIBRULE)

#$(LIBDIR)/linkm.h: linkm.h
#	rm -f $@
#	cp $? $@
#	chmod +r $@

$(OBJ): #linkm.h

try: try.o $(DEPLINKMLIB)
	$(CC) $(LDFLAGS) try.o $(LINKMLIB) -o try

try2: try2.o $(DEPLINKMLIB)
	$(CC) $(LDFLAGS) try2.o $(LINKMLIB) -o try2
