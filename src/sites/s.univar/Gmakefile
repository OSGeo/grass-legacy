PGM=s.univar
 
LIBES = $(GISLIB) $(DATETIMELIB)

LIST  = \
	univar.o \
	main.o \

$(BIN_CMD)/$(PGM): $(LIST) $(LIBES) 
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)

main.o:       univar.h 
univar.o:     univar.h
