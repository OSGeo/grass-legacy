interface_build {
    {v.in.dxf3d.sh} 0
    {Import contour levels and master contour levels with Z values to GRASS vector map.}
    {entry dxf {ASCII DXF file with contour levels:} 0 file}
    {entry lines {DXF file layer(s) with contour levels and Z values, and output GRASS vector fil(e) (name,name)} 0 ""}
}
