interface_build {
    {v.area} 0
    {Displays area and perimeter information for GRASS vector map.}
    {entry map {Vector map:} 0 vector}
    {entry color {Color [red]:} 0 color}
    {checkbox -f {Fill selected area with color (not simply outline).} "" -f}
    {checkbox -i {Exclude islands (holes) from polygon area.} "" -i}
}
