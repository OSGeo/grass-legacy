interface_build {
    m.lulc.USGS 0
    {Creates raster map layers from a Composite Theme Grid (CTG) file created by m.lulc.read. m.lulc.read extracts the CTG data from an ASCII landuse/landcover (lulc) CTG format file supplied by the USGS.}
    {entry -input {Input file:} 0 file}
}
