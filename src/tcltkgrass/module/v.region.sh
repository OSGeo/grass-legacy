interface_build {
    {v.region.sh} 0
    {Generate vector map with rectangular area for current region.}
    {entry map {Output new vector map:} 0 vector}
    {checkbox -s {Automatically run v.support.} "" -s}
    {checkbox -l {Automatically label vector area.} "" -l}
    {checkbox -v {Run verbosely.} "" -v}
}
