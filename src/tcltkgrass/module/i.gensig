interface_build {
    {i.gensig} 0
    {Generate non-interactively statistics for i.maxlik from raster map layer.}
    {entry group {Image group:} 0 group}
    {entry subgroup {Image subgroup:} 0 subgroup}
    {entry signaturefile {Signaturefile to be created:} 0 signature}
    {entry trainingmap {Ground truth training raster map:} 0 raster}
}
