interface_build {
    {r.out.tiff} 0
    {Exports a GRASS raster file into a TIFF (8/24bit) file.}
    {entry input {GRASS raster file (input):} 0 raster}
    {entry output {TIFF file (output):} 0 File}
    {checkbox -p {TIFF pallete output (8bit instead of 24bit).} "" -p}
    {checkbox -t {Output TIFF world file.} "" -t}
    {checkbox -l {Output Tiled TIFF.} "" -l}
    {checkbox -v {Verbose mode.} "" -v}
}
