interface_build {
    {v.stds.meta.cp} 0
    {Installs STDS supplementary metadata file for use by v.out.stds.}
    {entry map {Vector map to which metadata apply:} 0 vector}
    {entry metafile {File to be installed as metadata file:} 0 file}
    {checkbox -f {Overwrite existing file.} "" -f}
}
