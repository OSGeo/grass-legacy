interface_build {
    {r.cats} 0
    {Prints category values and labels associated with user-specified raster map layers.}
    {entry map {Raster map:} 0 raster}
    {entry cats {Category list (e.g. 1,3-8,13):} 0 ""}
    {entry fs {Input field separator (1 character) [space]:} 0 ""}
}
