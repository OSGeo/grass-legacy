interface_build {
    {r.in.tiff} 0
    {Imports a 8bit - TIFF file as a GRASS raster file.}
    {entry input {8bit - TIFF file (input):} 0 file}
    {entry output {GRASS raster file (output):} 0 raster}
    {checkbox -v {Verbose mode.} "" -v}
}
