interface_build {
    {i.tape.mss.h} 0
    {An imagery function that extracts header information from LANDSAT Multispectral Scanner (MSS) imagery data stored on half-inch tape.}
    {entry input {Name of the tape device:} 0 file}
}
