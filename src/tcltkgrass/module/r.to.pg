interface_build {
    {r.to.pg.tcl} 0
    {Exports a GRASS raster map to PostgreSQL table.}
    {entry map {Input raster map to export:} 0 raster}
    {entry output {PostgreSQL table to create:} 0 file}
}
