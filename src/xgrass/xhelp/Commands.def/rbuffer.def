

r.buffer - 

r.buffer creates a new @glossary("raster",16.glossary/rastdata.def) @glossary("map layer",16.glossary/maplayer.def) showing buffer (aka,
"distance" or "proximity") zones around all @glossary("cells",16.glossary/cell.def) that contain
non-zero @glossary("category values",16.glossary/catval.def) in an existing raster map layer.  The
distances of buffer zones from cells with non-zero category values are
user-chosen.  

Manual entry for @man("r.buffer")
