

d.save - 

d.save creates a shell script that can be used to recreate graphics in
another @glossary("frame",16.glossary/graphframe.def) at another time.  The shell script is sent to standard
output (the user's terminal) and can be saved by redirecting the
its output to a file.

Manual entry for @man("d.save")
