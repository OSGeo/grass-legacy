

r.reclass - 

r.reclass creates an output map layer based on an input @glossary("raster",16.glossary/rastdata.def) map
layer.  The output map layer will be a reclassification of the input
map layer based on reclass rules input to r.reclass and be treated in
much the same way that raster files are treated.

Manual entry for @man("r.reclass")
