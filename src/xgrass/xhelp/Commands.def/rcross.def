

r.cross - 

r.cross creates an output raster map layer representing all unique
combinations of @glossary("category values",16.glossary/catval.def) in the raster input layers.  At least
two, but not more than ten, input map layers must be specified.  The
user must also specify a name to be assigned to the output raster map
layer created by r.cross.

Manual entry for @man("r.cross")
