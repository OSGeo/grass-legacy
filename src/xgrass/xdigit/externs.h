

char *tell_type ();	/* return pointer to type description */
char *help_file_name (); /* return pointer to path name of help file */
double unit_conversion ();

double _map_to_dig_thresh ();  /* convert map thresh to digitizing thresh */
plus_t ** dig_get_cont_lines ();
