all:
	rm -rf $(SCRIPTS)/demo.scripts
	if [ ! -d $(SCRIPTS) ]; then mkdir $(SCRIPTS); fi
	cp shells/*.sh $(SCRIPTS)
	mkdir $(SCRIPTS)/demo.scripts
	(cd shells/demo.scripts/ ; tar -cf - * | (cd $(SCRIPTS)/demo.scripts ; tar -xf - 2>/dev/null ); true )
	$(GMAKE) contrib
	chmod -R 0755 $(SCRIPTS)/*
