PGM  = m.dted.examine
HOME = $(BIN_MAIN_CMD)

LIST	= main.o tape.o 

$(HOME)/$(PGM): $(LIST) $(GISLIB)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(GISLIB) $(MATHLIB) $(XDRLIB)


$(GISLIB):      # in case library changes
