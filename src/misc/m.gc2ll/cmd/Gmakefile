PGM=m.gc2ll
LIBES = $(GISLIB) $(COORCNVLIB)
DEPLIBS= $(DEPGISLIB) $(DEPCOORCNVLIB)

LIST = main.o sph.o

$(BIN_CMD)/$(PGM): $(LIST) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)

$(DEPLIBS): # in case LIBs change
