PGM=m.ll2u

LIST = main.o input.o output.o warning.o sph.o number.o

$(BIN_MAIN_CMD)/$(PGM): $(LIST) $(COORCNVLIB) $(GISLIB)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(COORCNVLIB) $(GISLIB) $(MATHLIB) $(XDRLIB)

$(COORCNVLIB): # in case LIB changes
$(GISLIB): # in case LIB changes
