PGM = m.datum.shift
LIBES = $(COORCNVLIB) $(GISLIB)
DEPLIBS= $(DEPCOORCNVLIB) $(DEPGISLIB)
LIST = main.o \
       dat.o \
       sph.o
# EXTRA_CFLAGS=-DBURSAWOLF

$(BIN_CMD)/$(PGM): $(LIST) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)

$(DEPGISLIB): # in case LIB changes
$(DEPCOORCNVLIB): # in case LIB changes
