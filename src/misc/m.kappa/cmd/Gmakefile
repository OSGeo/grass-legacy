PGM=m.kappa

LIBES= $(GISLIB) $(VASK)

OBJS = main.o\
	calc_kappa.o\
	prt_hdr.o\
	readin_data.o

$(BIN_ALPHA_CMD)/$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(MATHLIB) $(XDRLIB)

$(LIBES): #
