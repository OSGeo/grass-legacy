PGM=m.kappa

LIBES= $(GISLIB) $(VASKLIB)

OBJS = main.o\
	calc_kappa.o\
	prt_hdr.o\
	readin_data.o

$(BIN_ALPHA_CMD)/$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(CURSES) $(MATHLIB) $(XDRLIB)

$(LIBES): #
