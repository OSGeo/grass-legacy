PGM=m.ll2db
EXTRA_CFLAGS=$(PROJINC)

LIST = main.o input.o output.o warning.o number.o

$(BIN_CMD)/$(PGM): $(LIST) $(DEPGPROJLIB) $(DEPGISLIB)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(GPROJLIB) $(GISLIB) $(MATHLIB) $(XDRLIB)

$(DEPCOORCNVLIB): # in case LIB changes
$(DEPGISLIB): # in case LIB changes
