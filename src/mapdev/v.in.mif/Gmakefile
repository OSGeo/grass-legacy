PGM = v.in.mif

LIST = 	lex.yy.o \
	main.o \
	attval.o \
	parse.o \
	write_lines.o \
	fields.o \
	cpoly.o \
	gbtree.o \
	store.o

LIBES=$(VECTLIB) $(DIG_ATTLIB) $(DATETIMELIB) $(VASKLIB) $(GISLIB)

$(BIN_CMD)/$(PGM): $(LIST) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(LINKMLIB) $(CURSES) $(MATHLIB) $(XDRLIB)

lex.yy.c : mif.l
	$(LEX) mif.l

$(VECTLIB): #
$(GISLIB): # 
$(DIG_ATTLIB): #
