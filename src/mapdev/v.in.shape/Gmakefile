PGM = v.in.shape
HOME = $(BIN_CONTRIB_CMD)
EXTRA_CFLAGS = $(VECT_INCLUDE)
# EXTRA_CFLAGS = $(VECT_INCLUDE) -DDEBUG # for debugging
LIST = 	main.o \
	shpopen.o \
	dbfopen.o \
	dbutils.o \
	shp2dig.o \
	writelin.o \
	gbtree.o \
	cleanup.o

$(HOME)/$(PGM): $(LIST) $(GISLIB) $(DATETIMELIB)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(GISLIB) $(VECTLIB) $(VASK) $(MATHLIB) $(DATETIMELIB) $(XDRLIB)

$(GISLIB): # 
