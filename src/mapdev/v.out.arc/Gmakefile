PGM =	v.out.arc

OBJ =	Vexport_arc.o\
	write_areas.o\
	write_lines.o\
	write.o\
	writ_a_lines.o

LIBES=$(VECTLIB) $(GISLIB) $(DIG_ATTLIB)

$(BIN_ALPHA_CMD)/$(PGM): $(OBJ) $(LIBES)
	$(CC) -o $@  $(LDFLAGS) $(OBJ) $(LIBES) $(MATHLIB) $(XDRLIB)

$(VECTLIB): #
$(DIG_ATTLIB): #
$(GISLIB): #
