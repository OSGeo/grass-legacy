 DISPLAY NODE LINES     Display each line attached to a given node.
   Select node with mouse.  Then right button shows lines counter-clockwise,
 left button clockwise.  This is useful for finding lines that have been
 digitized twice.
