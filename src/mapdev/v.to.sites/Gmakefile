EXTRA_CFLAGS	= $(VECT_INCLUDE)



PROG =  $(BIN_MAIN_CMD)/v.to.sites

all:  $(PROG)
	


$(PROG): v.to_sites.o $(GISLIB) $(VECTLIB)
	rm -f a.out $@
	$(CC) $(LDFLAGS) v.to_sites.o $(GISLIB) $(VECTLIB) $(MATHLIB)
	mv a.out $@

saber_src:
	#load $(EXTRA_CFLAGS) $(CFLAGS) ../Vlib/*.c ../diglib/*.c v.to_sites.c $(GISLIB) $(DIG_ATTLIB) $(MATHLIB)

