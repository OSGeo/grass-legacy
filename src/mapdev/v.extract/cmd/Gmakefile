PGM = v.extract

OFILES = \
	main.o \
	xtract_area.o \
	xtract_line.o

LIBES=$(VECTLIB) $(GISLIB) $(DIG_ATTLIB)
$(BIN_CMD)/$(PGM): $(OFILES) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OFILES) $(LIBES) $(MATHLIB) $(XDRLIB)

$(LIBES): #
