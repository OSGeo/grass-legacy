PGM=v.patch
LIBES = $(VECTLIB) $(DIG_ATTLIB) $(GISLIB)

SOURCES = \
	patch.c

OBJ = \
	patch.o

$(BIN_MAIN_CMD)/$(PGM): $(OBJ) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJ) $(LIBES) $(MATHLIB) $(XDRLIB)

$(LIBES): #
