 LABEL CONTOURS
    Use to label several contours.  Choose 2 outside contours and provide
 their values (unless already labelled) and will label all contours between
 them based on contour interval <i>.  Will not work if values area incorrect.
