PGM = v.what
OFILES = \
	main.o \
	openvect.o\
	what.o

# VECTLIB entry *twice* needed:
LIBES=$(VECTLIB) $(VECTLIB) $(DISPLAYLIB) $(RASTERLIB) $(GISLIB)

$(BIN_MAIN_CMD)/$(PGM): $(OFILES) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OFILES) $(LIBES) $(MATHLIB) $(XDRLIB)

$(RASTERLIB): #
$(DISPLAYLIB): #
$(GISLIB): #
$(VECTLIB): #
