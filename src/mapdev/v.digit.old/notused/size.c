#include "digit.h"

main ()
{
    fprintf (stderr, "P_LINE: %d\n", sizeof (P_LINE));
    fprintf (stderr, "P_AREA: %d\n", sizeof (P_AREA));
    fprintf (stderr, "P_NODE: %d\n", sizeof (P_NODE));
    fprintf (stderr, "P_ATT: %d\n", sizeof (P_ATT));
}
