PGM=d.where
EXTRA_CFLAGS=$(PROJINC)

LIBES = $(DISPLAYLIB) $(RASTERLIB) $(GPROJLIB) $(GISLIB)
DEPLIBS= $(DEPDISPLAYLIB) $(DEPRASTERLIB) $(DEPGPROJLIB) $(DEPGISLIB)

LIST = \
	b_w_line.o \
	main.o \
	where.o


$(BIN_CMD)/$(PGM): $(LIST) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@  $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)


$(DEPLIBS): #
