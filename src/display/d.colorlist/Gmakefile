PGM=d.colorlist

LIBES=$(GISLIB) $(DISPLAYLIB)
DEPLIBS=$(DEPGISLIB) $(DEPDISPLAYLIB)

OFILES	= \
	main.o

$(BIN_CMD)/$(PGM): $(OFILES) $(DEPLIBS)
	$(CC) ${LDFLAGS} -o $@ $(OFILES) $(LIBES) $(MATHLIB) $(XDRLIB)

$(DEPGISLIB): #
$(DEPDISPLAYLIB): #
