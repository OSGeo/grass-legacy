PGM = d.rast.edit
EXTRA_CFLAGS	= $(DANOTHER_BUTTON)

OFILES = \
	main.o \
        menu.o \
	mk_tmp_file.o\
	mk_new_layer.o\
	draw_grid.o\
	edit.o

LIBES=$(D_LIB) $(DISPLAYLIB) $(RASTERLIB) $(GISLIB)
$(BIN_INTER)/$(PGM): $(OFILES) $(LIBES)
	$(CC) -o $@ $(LDFLAGS) $(OFILES) $(LIBES) $(CURSES) $(MATHLIB) $(XDRLIB)

$(OFILES): edit.h

$(RASTERLIB): #
$(DISPLAYLIB): #
$(GISLIB): #
$(D_LIB): #
