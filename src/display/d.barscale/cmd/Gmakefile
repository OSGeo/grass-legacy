PGM=d.barscale

OFILES = \
	draw_scale.o \
	mouse.o \
	main.o
LIBES=$(DISPLAYLIB) $(RASTERLIB) $(GISLIB)
DEPLIBS=$(DEPDISPLAYLIB) $(DEPRASTERLIB) $(DEPGISLIB)

$(BIN_CMD)/$(PGM): $(OFILES) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(OFILES) $(LIBES) $(MATHLIB) $(XDRLIB)

$(DEPRASTERLIB): #
$(DEPDISPLAYLIB): #
$(DEPGISLIB): #

draw_scale.o:	options.h
main.o:		options.h
mouse.o:	options.h
