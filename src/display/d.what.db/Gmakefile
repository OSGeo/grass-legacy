DBMILIB = $(LIBDIR)/libdbmi.a 
LIBES  = $(DISPLAYLIB) $(RASTERLIB) $(GISLIB) $(DATETIMELIB) $(MATHLIB) $(DBMILIB)

PGM    = d.what.db
HOME   = $(BIN_CONTRIB_CMD)

LIST   = main.o \
	 show.o \
	 attr.o

$(HOME)/$(PGM): $(LIST) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(XDRLIB)

$(LIBES): #
