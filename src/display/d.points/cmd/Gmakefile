PGM =d.points
OBJ = main.o draw.o setup.o

LIBES = $(DISPLAYLIB) $(RASTERLIB) $(GISLIB)
$(BIN_CMD)/$(PGM): $(OBJ) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJ) $(LIBES) $(MATHLIB) $(XDRLIB)

$(LIBES): #

$(OBJ): globals.h
