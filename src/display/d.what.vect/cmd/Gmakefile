PGM = d.what.vect
OFILES = \
	attr.o \
	main.o \
	openvect.o\
	what.o


all: $(BIN_CMD)/$(PGM)

DBMILIB = $(LIBDIR)/libdbmi.a
LIBES=$(VECTLIB) $(DISPLAYLIB) $(RASTERLIB) $(GISLIB) $(DBMILIB)
DEPLIBS=$(DEPVECTLIB) $(DEPDISPLAYLIB) $(DEPRASTERLIB) $(DEPGISLIB) $(DBMILIB)

$(BIN_CMD)/$(PGM): $(OFILES) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(OFILES) $(LIBES) $(MATHLIB) $(XDRLIB)

$(DEPRASTERLIB): #
$(DEPDISPLAYLIB): #
$(DEPGISLIB): #
$(DEPVECTLIB): #
