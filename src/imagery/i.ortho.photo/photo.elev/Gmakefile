PROGRAM      = photo.elev
HOME        = $(ETC)/imagery

EXTRA_CFLAGS = -I../libes
IMAGERYLIB2  = ../libes/libortho.a

LIST = main.o ask_elev.o mod_elev.o

LIBES=$(IMAGERYLIB2) $(IMAGERYLIB) $(GISLIB) $(VASK)

$(HOME)/$(PROGRAM): $(LIST) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)

$(IMAGERYLIB2) $(LIBES): #

main.o: elev.h
ask_elev.o: elev.h
mod_elev: elev.h

