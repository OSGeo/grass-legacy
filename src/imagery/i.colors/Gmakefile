PGM=i.colors
LIST = main.o \
	bars.o\
	colors.o\
	driver.o\
	graphics.o\
	image.o\
	input.o\
	info.o\
	mouse.o\
	title.o

LIBES = $(IMAGERYLIB) $(D_LIB) $(DISPLAYLIB) $(RASTERLIB) $(GISLIB) $(VASKLIB)

$(BIN_INTER)/$(PGM): $(LIST) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(CURSES) $(MATHLIB) $(XDRLIB)

$(LIST): globals.h defs.h

$(LIBES): #

