PGM = i.quantize

LIBES = $(IMAGERYLIB) $(GISLIB) $(VASKLIB)

OBJ =\
	main.o\
	ask_levels.o\
	compose.o\
	gen_colors.o \
	median.o

$(BIN_INTER)/$(PGM): ${OBJ}
	$(CC) ${LDFLAGS} -o $@ ${OBJ} ${LIBES} $(CURSES) $(MATHLIB) $(XDRLIB)

$(IMAGERYLIB) $(GISLIB) $(VASK): #
