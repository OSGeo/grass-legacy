PGM = i.quantize

LIBES = $(IMAGERYLIB) $(GISLIB) $(VASK)

OBJ =\
	main.o\
	ask_levels.o\
	compose.o\
	gen_colors.o \
	median.o

$(BIN_ALPHA_INTER)/$(PGM): ${OBJ}
	$(CC) ${LDFLAGS} -o $@ ${OBJ} ${LIBES} $(MATHLIB) $(XDRLIB)

$(IMAGERYLIB) $(GISLIB) $(VASK): #
