PGM=i.in.gtc
LIST	= main.o

LIBES= $(IMAGERYLIB) $(GISLIB) $(VASK)

$(BIN_MAIN_INTER)/$(PGM): $(LIST) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)

$(LIBES):	# in case library changes

$(LIST):	gtc.h
