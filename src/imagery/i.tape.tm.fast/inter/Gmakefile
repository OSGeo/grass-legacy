LIBES = $(IMAGERYLIB) $(GISLIB) $(VASK)

LIST = main.o\
	ask_options.o\
	ask_title.o\
	ask_window.o\
	tm_extract.o

PGM = i.tape.tm.fast

$(BIN_ALPHA_INTER)/$(PGM): $(LIST) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)

$(LIST): tape.h

$(LIBES): #
