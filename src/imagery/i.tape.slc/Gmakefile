LIST	= main.o\
	  advance.o\
	  tapename.o\
	  mount_tape.o

LIBES= $(IMAGERYLIB) $(GISLIB)

$(BIN_MAIN_INTER)/i.tape.slc: $(LIST) $(LIBES) $(VASKLIB)
	$(CC) $(LDFLAGS) $(LIST) $(LIBES) $(VASK) -o $@ $(MATHLIB) $(XDRLIB)

$(LIBES):	# in case library changes
$(VASKLIB):	# in case library changes

$(LIST):	tape.h
