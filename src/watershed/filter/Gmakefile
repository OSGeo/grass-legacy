# %W% %G%

PGM=filtermap

#FRONT = frontend.o ask_int.o
BACK = main.o filter.o percent.o die.o set_window.o usage.o

#all:  $(BIN)/filtermap $(BIN)/Gfiltermap

#$(BIN)/filtermap: $(FRONT) $(GISLIB)
#	$(CC) $(LDFLAGS) $(FRONT) $(GISLIB) $(TERMLIB) -o $@

$(ETC)/Gfiltermap: $(BACK) $(GISLIB)
	$(CC) $(LDFLAGS) $(BACK) $(GISLIB) $(TERMLIB) -o $@

$(GISLIB): #
