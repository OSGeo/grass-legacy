# %W%  %G%

LIB = $(LIBDIR)/libcoorcnv.a
EXTRA_CFLAGS = $(VECT_INCLUDE)

all:	mytest

mytest: mytest.o
	$(CC) $(LDFLAGS) mytest.o $(LIB) $(DIG_ATTLIB) $(VECTLIB) \
		$(GISLIB) $(MATHLIB) -o $@

$(LIB): # in case LIB changes
$(GISLIB): #
$(VECTLIB): #
$(DIG_ATTLIB): #
