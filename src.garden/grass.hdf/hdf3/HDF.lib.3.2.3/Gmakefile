HDF_LIBDIR = lib/$(LIBARCH)
HDFLIB = lib/libdf.a


all: $(HDF_LIBDIR)
	$(MAKE) libnofortran
	mv lib/libdf.\a $(HDFLIB)
	$(MAKE) clean

$(HDF_LIBDIR):
	if [ ! -d lib ]; then mkdir lib ; fi
	mkdir $(HDF_LIBDIR)
