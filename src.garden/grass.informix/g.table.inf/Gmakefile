PGM       = $(BIN_CMD)/g.table.inf
LIBES     = $(GISLIB)
DEPLIBS= $(DEPGISLIB)



OBJS = \
	main.o \
	infxTable.o \
	delcr.o

$(PGM): $(OBJS) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(XDRLIB) $(MATH)

$(DEPLIBS): #

