PGM       = $(BIN_MAIN_CMD)/v.reclass.inf
#PGM       = $(BIN_MAIN_CMD)/v.r.inf.new
LIBES     = $(GISLIB) 



OBJS = \
	main.o  \
	runSQL.o \
	row_or_col.o \
	readSQLout.o

#EXTRA_CFLAGS =  -Dunlink=puts


$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) $(EXTRA_CFLAGS) -o $@ $(OBJS) $(LIBES) $(XDRLIB) $(MATHLIB)

$(LIBES): #
