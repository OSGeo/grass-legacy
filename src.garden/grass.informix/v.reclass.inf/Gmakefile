PGM       = $(BIN_CMD)/v.reclass.inf
#PGM       = $(BIN_CMD)/v.r.inf.new
LIBES     = $(GISLIB) 
DEPLIBS= $(DEPGISLIB) 



OBJS = \
	main.o  \
	runSQL.o \
	row_or_col.o \
	readSQLout.o

#EXTRA_CFLAGS =  -Dunlink=puts


$(PGM): $(OBJS) $(DEPLIBS)
	$(CC) $(LDFLAGS) $(EXTRA_CFLAGS) -o $@ $(OBJS) $(LIBES) $(XDRLIB) $(MATHLIB)

$(DEPLIBS): #
