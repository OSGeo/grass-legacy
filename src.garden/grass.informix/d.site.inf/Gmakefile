PGM       = $(BIN_MAIN_CMD)/d.site.inf
LIBES     = $(DISPLAYLIB) $(RASTERLIB) $(GISLIB) $(DATETIMELIB)

# EXTRA_CFLAGS = -Dunlink=puts

OBJS = \
	main.o   \
        getAllOpts.o \
        getSopts.o \
        buildInfxQry.o \
        runInfxFile.o \
	plotsite.o

$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(MATHLIB) $(XDRLIB)


$(LIBES): #
