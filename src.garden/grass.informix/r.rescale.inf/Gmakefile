PGM       = $(BIN_CMD)/r.rescale.inf
LIBES     = $(GISLIB)
DEPLIBS= $(DEPGISLIB)

# EXTRA_CFLAGS	= -Dunlink=puts



OBJS = \
	main.o \
	infxQry.o 


$(PGM): $(OBJS) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(XDRLIB) $(MATH)

$(DEPLIBS): #
