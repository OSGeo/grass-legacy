PGM       = $(BIN_MAIN_CMD)/g.stats.inf
LIBES     = $(GISLIB)

#EXTRA_CFLAGS	= -Dunlink=puts


OBJS = \
	main.o \
	infxStats.o \
	chktype.o


$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(XDRLIB) $(MATH)

$(LIBES): #
