PGM       = $(BIN_CMD)/g.stats.inf
LIBES     = $(GISLIB)
DEPLIBS= $(DEPGISLIB)

#EXTRA_CFLAGS	= -Dunlink=puts


OBJS = \
	main.o \
	infxStats.o \
	chktype.o


$(PGM): $(OBJS) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(XDRLIB) $(MATH)

$(DEPLIBS): #
