PGM       = $(BIN_MAIN_CMD)/r.rescale.ora
LIBES     = $(GISLIB)

# EXTRA_CFLAGS	= -Dunlink=puts



OBJS = \
	main.o \
	infxQry.o 


$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES)

$(LIBES): #
