PGM       = $(BIN_MAIN_CMD)/g.stats.ora
LIBES     = $(GISLIB)

#EXTRA_CFLAGS	= -Dunlink=puts


OBJS = \
	main.o \
	oraStats.o \
	chktype.o


$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(XDRLIB) $(MATHLIB)

$(LIBES): #
