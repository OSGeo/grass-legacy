PGM       = $(BIN_CMD)/g.stats.ora
LIBES     = $(GISLIB)
DEPLIBS= $(DEPGISLIB)

#EXTRA_CFLAGS	= -Dunlink=puts


OBJS = \
	main.o \
	oraStats.o \
	chktype.o


$(PGM): $(OBJS) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(XDRLIB) $(MATHLIB)

$(DEPLIBS): #
