PGM       = $(BIN_CMD)/d.rast.ora
LIBES     = $(GISLIB) 
DEPLIBS= $(DEPGISLIB) 
# EXTRA_CFLAGS = -Dunlink=puts




OBJS = \
	main.o \
        getAllOpts.o \
        getSopts.o \
	buildSQL.o \
	runSQL.o \
	readSQLout.o \
	row_or_col.o 


$(PGM): $(OBJS) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(XDRLIB) $(MATHLIB)

$(DEPLIBS): #
