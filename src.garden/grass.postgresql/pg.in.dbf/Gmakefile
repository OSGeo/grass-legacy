PGM = pg.in.dbf
HOME = $(BIN_CMD)
PACKAGE = "pg.in.dbf"
DEFS = -Wall -DPACKAGE=\"$(PACKAGE)\"
EXTRA_CFLAGS    = $(PQINCPATH) $(DEFS)

LIST = 	main.o \
	dbfopen.o \
	pgdump.o

$(HOME)/$(PGM): $(LIST) $(DEPGISLIB) $(DEPDATETIMELIB)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(GISLIB) $(VECTLIB) \
		$(DATETIMELIB) $(MATHLIB) $(XDRLIB) \
		$(PQLIBPATH) $(PQLIB) $(INTLLIB)

$(DEPGISLIB): # 
