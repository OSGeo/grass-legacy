PGM       = $(BIN_CMD)/g.select.pg
LIBES     = $(GISLIB)
LOCALEDIR = $(INST_DIR)/locale
PACKAGE = "g.select.pg"
DEFS = -Wall -DLOCALEDIR=\"$(LOCALEDIR)\" -DPACKAGE=\"$(PACKAGE)\"
EXTRA_CFLAGS    = $(PQINCPATH) $(DEFS)

OBJS = \
	main.o \
	getdbname.o \
	listdb.o


$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES) $(PQLIBPATH) $(PQLIB) $(XEXTRALIBS) $(MATHLIB) $(XDRLIB)

$(LIBES): #
