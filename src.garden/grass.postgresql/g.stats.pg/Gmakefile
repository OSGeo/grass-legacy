PGM       = $(BIN_CMD)/g.stats.pg
LIBES     = $(GISLIB)
EXTRA_CFLAGS = $(PQINCPATH)
#EXTRA_CFLAGS	= -Dunlink=puts

OBJS = \
	main.o \
	infxStats.o

$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES)  $(PQLIBPATH) $(PQLIB) $(XEXTRALIBS) $(MATHLIB) $(XDRLIB)

$(LIBES): #
