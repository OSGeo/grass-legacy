#PGM       = g.stats.pg
PGM       = $(BIN_CONTRIB_CMD)/g.stats.pg
LIBES     = $(GISLIB) $(PQLIB) $(MATHLIB)
EXTRA_CFLAGS = $(PQINCPATH)

#EXTRA_CFLAGS	= -Dunlink=puts


OBJS = \
	main.o \
	infxStats.o \
	chktype.o


$(PGM): $(OBJS) $(LIBES)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LIBES)

$(LIBES): #
