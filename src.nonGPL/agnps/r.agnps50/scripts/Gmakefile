all:
	cp r.agnps50.input $(GISBASE)/scripts/
	if [ ! -d $(GISBASE)/etc/agnps50 ]; then mkdir $(GISBASE)/etc/agnps50 ; fi
	(tar --exclude=CVS -cvf - * | tar -C $(GISBASE)/etc/agnps50 -xvf - 2>/dev/null ; true )
