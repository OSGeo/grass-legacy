.\" %W% %G%
.TH r.weighted.cn G-language
.SH NAME
r.weighted.cn \- Generates a weighted SCS curve number map layer
.br
.I (G Language Tool)

.SH DESCRIPTION
.B r.weighted.cn
is a command-line interface for generating a weighted SCS CN map.
The command format is:

r.weighted.cn input=cn_map output=weighted_cn_map

.nf
input=map	curve number map name
output=map	weighted curve number map name
.fi

The command-line ordering can be in any form but all key
words must be there to run the program.

.SH EXAMPLE
r.weighted.cn input=cn_name output=weighted_cn

.RS
will create a weighted CN map weighted_cn
.RE

.SH NOTE
.I The r.weighted.cn
program is sensitive to the \fBcurrent window setting\fR. Thus the
program can be used to generate a weighted CN map of any sub-area 
within the full map layer. Also, \fIr.weighted.cn\fR is sensitive to
any \fImask\fR in effect.

.SH SEE ALSO
GCN, CN

.SH "AUTHORS"
Raghavan Srinivasan and Dr. Bernie A. Engel, Agricultural Engineering Department, Purdue University
