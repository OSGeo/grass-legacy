PGM    = r.fill
OBJS   = main.o
LIBDIR = $(UNUSED)/libes
LIBTIR = $(LIBDIR)/libtir
CFLAGS+= -I$(LIBTIR)
LIBES  = $(GISLIB) $(LIBDIR)/libtir.a

$(BIN_CMD)/$(PGM): $(OBJS) $(LIBES)
	$(CC) -o $@ $(OBJS) $(LDFLAGS) $(LIBES) $(MATHLIB) $(XDRLIB)

