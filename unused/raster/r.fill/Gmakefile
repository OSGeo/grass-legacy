PGM    = r.fill
OBJS   = main.o
LIB    = $(UNUSED)/libes
LIBTIR = $(LIB)/libtir
CFLAGS+= -I$(LIBTIR)
LIBES  = $(GISLIB) $(LIB)/libtir.a

$(BIN_CMD)/$(PGM): $(OBJS) $(LIBES)
	$(CC) -o $@ $(OBJS) $(LDFLAGS) $(LIBES) $(MATHLIB) $(XDRLIB)

