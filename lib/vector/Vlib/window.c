#include "Vect.h"

/*
int 
Vect__get_window (
	   struct Map_info *Map, double *n, double *s, double *e, double *w)
{
  struct dig_head *dhead;

  dhead = &(Map->head);

  *n = dhead->N;
  *s = dhead->S;
  *e = dhead->E;
  *w = dhead->W;

  return (0);
}
*/
