CFLAGS = -Wall -DGNGRP_STATS -DGNGRP_NEWCLIP
LNFLAGS =
OBJECTS = avl.o tree.o heap.o graph.o
INCLUDES = avl.h tree.h heap.h graph.h type.h
LIBRARY = libdgl.a

$(LIBRARY): $(OBJECTS) $(INCLUDES)
	ar r $@ $(OBJECTS); ranlib $@

.c.o:
	cc -c $(CFLAGS) $< -o $@

clean:
	rm -f *.o *.a
