
OFILES = \
        main.o \
        clock.o

CFILES = \
         clock.c \
         main.c

all: $(BIN_CMD)/v.out.DI

$(BIN_CMD)/v.out.DI: $(OFILES) $(VECTLIB) $(GISLIB)
	$(CC) -o $@ $(LDFLAGS) $(OFILES) $(VECTLIB) $(GISLIB) $(XDRLIB) $(MATHLIB)


$(OFILES):      vtoc.h


saber_src:
	#load $(EXTRA_CFLAGS) $(CFLAGS) ../Vlib/*.c ../diglib/*.c $(CFILES) $(GISLIB) $(DIG_ATTLIB) $(MATHLIB)
