EXTRA_CFLAGS = $(VECT_INCLUDE) 

OFILES = \
        main.o \
        clock.o

CFILES = \
         clock.c \
         main.c

all: $(BIN_CONTRIB_CMD)/v.out.DI

$(BIN_CONTRIB_CMD)/v.out.DI: $(OFILES) $(VECTLIB) $(GISLIB)
	rm -f a.out $@
	$(CC) $(LDFLAGS) $(OFILES) $(VECTLIB) $(GISLIB) $(MATHLIB)
	mv a.out $@


$(OFILES):      vtoc.h


saber_src:
	#load $(EXTRA_CFLAGS) $(CFLAGS) ../Vlib/*.c ../diglib/*.c $(CFILES) $(GISLIB) $(DIG_ATTLIB) $(MATHLIB)
