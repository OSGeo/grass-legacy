PGM      = r3.info
HOME     = $(BIN_MAIN_CMD)

LIST = main.o

$(HOME)/$(PGM): $(LIST) $(G3DLIB) $(GISLIB)
	$(CC) $(LDFLAGS) $(LIST) $(G3DLIB) $(GISLIB) $(XDRLIB) $(MATHLIB) -o $@

$(GISLIB):      # in case library changes
$(G3DLIB):      # in case library changes
