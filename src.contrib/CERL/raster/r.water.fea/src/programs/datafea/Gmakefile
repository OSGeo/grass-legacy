PGM = datafea
RFEA=$(GISBASE)/etc/r.fea
LIBES = $(GISLIB)
DEPLIBS= $(DEPGISLIB)
LIST = data.o

$(RFEA)/$(PGM): $(LIST) $(DEPLIBS)
	$(CC) $(LDFLAGS) -o $@ $(LIST) $(LIBES) $(MATHLIB) $(XDRLIB)

$(DEPLIBS): #
