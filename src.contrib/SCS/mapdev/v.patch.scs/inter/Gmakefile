MAPDEV		= $(SRC)/mapdev

EXTRA_CFLAGS	= -I$(MAPDEV)/diglib   -I$(MAPDEV)/libes

IFILES = \
	patch.o

all:  $(BIN_INTER)/v.patch.scs

$(BIN_INTER)/v.patch.scs: $(IFILES)
	$(CC) $(LDFLAGS) -o $@ $(IFILES) $(GISLIB) $(CURSES) $(MATHLIB) $(XDRLIB)
