 LABEL: AREAS, LINES, or SITES
   Enter area, line, or site label number.  Select and accept each 
   area, line, or site to be labeled.  Use abort to quit.
 LABEL: MULTIPLE LINES 
   Attempts to follow lines when labeling.  Will span multiple lines
   across nodes.  Will follow if only 2 lines meet at node, or the line 
   that best matches the angle of the entering line.
 LABEL: CONTOURS
    Use to label several contours.  Choose 2 outside contours and provide
    their values (unless already labelled) and will label all contours 
    between them based on contour interval.  Will not work if values are 
    incorrect.
