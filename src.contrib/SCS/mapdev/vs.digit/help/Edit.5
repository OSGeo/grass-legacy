 SNAP LINE TO LINE    Selectively intersect two lines.
   Use the mouse to select: 1) a line to be broken, 2) a break point,
   and 3) the line to snap to the break.  Left button to select,
 Right button to accept, Middle button to abort/quit when finished. 
