#include <stdio.h>
#include "polish.h"
int 
myprint (double ***y, double ***yold)
{
  int i, j;
/*
  for (i = 23; i >= 0; --i)
  {
    for (j = 0; j < 17; ++j)
      if (y[i][j][0] != EMPTY_CELL)
	fprintf (stderr, "%02.2f\t", y[i][j][0]);
      else
	fprintf (stderr, "      \t");
    fprintf (stderr, "\n");
  }
*/
  return 0;
}
