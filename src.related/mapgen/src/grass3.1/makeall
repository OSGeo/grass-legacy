#!/bin/sh
if test ! -d $GRAPH/macros
then
	mkdir $GRAPH/macros
fi
cd libes
rm *.a 2> /dev/null
rm *.o 2> /dev/null
$GRASS/src/CMD/Gmake
cd ..
for i in `ls`
do
if test -d $i
then
cd $i
if test -f Gmakefile; then
	rm *.o 2> /dev/null
	$GRASS/src/CMD/Gmake
fi
rm *.o
cd ..
fi
done
