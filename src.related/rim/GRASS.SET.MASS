# setup src for MASSCOMP systems
cp BSD/*.f src
cp BSD/*.d src
# always do next steps after copying BSD sources
cp MASS/*.f src
cp MASS/*.d src
# build the Makefile in this directory for manual use later
cat <<EOF >Makefile
# definitions for MASSCOMP rim
PILE = f77
EXTRA =
LIBEXTRA =
AR = ar cq
RAN = ranlib 
EOF
# build the Makefile in src directory
cp Makefile src
cat Makefile-generic >>Makefile
cat src/Makefile-generic >>src/Makefile
