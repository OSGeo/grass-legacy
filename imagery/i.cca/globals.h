
#define MX 9
#define MC 50

#ifdef MAIN
char outputfile[50];
char groupname[50], subgroup[50], signame[50];
#else
extern char outputfile[50];
extern char groupname[50], subgroup[50], signame[50];
#endif
